#!/bin/zsh

# az login

# az acr login --name cachebusters
#TOKEN=$(az acr login --name cachebusters  --expose-token --output tsv --query accessToken)
az acr login --name codebreakers
az acr login --name whowho
az acr login --name bannerflow
az acr login --name bfacr 
